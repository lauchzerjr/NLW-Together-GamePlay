const DATABASE_NAME = "@NLWTogetherGamePlay";

// @app:collection

const COLLECTION_USERS = `${DATABASE_NAME}:users`;
const COLLECTION_APPOINTMENTS = `${DATABASE_NAME}:appointments`;

export { COLLECTION_APPOINTMENTS, COLLECTION_USERS };
